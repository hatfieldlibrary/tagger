Express configuration maps foundation5 Javascript to the Angularjs module's bower_components/foundation directory.

Only application.js and jquery-ui files are served from this directory.  These files are mapped separately in Express
configuration (config/express.js).
