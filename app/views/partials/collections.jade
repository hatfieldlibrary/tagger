
md-content(class="transparent" layout="row", layout-align="start start",  ng-controller="CollectionCtrl as vm")
  md-content(class="transparent" layout="column" flex="75" class="transparent")
    md-card.flex(flex, class="primary-content" style="background: #DFE3EE;")
      div(ng-if="vm.collectionList.$resolved")
      md-toolbar.md_primary
        .md-toolbar-tools
          span(flex="25")
            i.material-icons() collections
            span.md-subhead &nbsp;COLLECTIONS
          span(flex="70")
            h3.md-display-1 {{vm.collection.title}}
          span(flex)
          md-button(class="md-warn md-raised md-fab md-mini" aria-label="Delete Collection" ng-click="vm.showDialog($event, vm.deleteMessage)") <i class="material-icons">delete</i>
      md-card-content
        div(layout="row", layout-align="start start")
          div(flex="20" )
            md-button(class="md-raised md-accent" style="margin-top: 15px;" ng-click="vm.showDialog($event, vm.addMessage)") Add Collection
            div(layout="column", style="height:770px; background: transparent;")
              md-content(flex style="background: transparent;")
                md-list
                div(ng-repeat="col in vm.collectionList")
                  md-list-item
                    md-button(class="md-no-style md-button  md-default-theme", ng-class="{'md-primary': col.collection.id==vm.collectionId}" style="padding-left: 5px;min-height: 47px;" ng-click="vm.getCollectionById(col.collection.id);")
                      div(class="list-group-item-text md-subhead layout-fill") {{col.collection.title}}
                        div.md-ripple-container
                  md-divider
          md-content(layout-padding flex="80" style="padding: 55px;border: 1px solid #ccc;background: #fff;" class="transparent")
            div(flex="80" class="md-subhead grey-text")
              span With the exception of item count and dates, all fields for this Collection must be complete.  You can use the default image if none is available. A minimum of one content type and one subject tag should be provided.
            div(flex)
            md-button(class="md-raised md-accent" style="float:right;" ng-click="vm.updateCollection()") Update
            div(flex)
              p &nbsp;
            md-input-container
              label Collection Name
              input(type="text", ng-model="vm.collection.title")
            md-input-container
              label Description  &nbsp;
                span(class="md-subhead md-default") maximum characters 450 (current {{vm.collection.description.length}})
              textarea(type="textarea", ng-trim="false", ng-model="vm.collection.description", maxlength="450")
            md-input-container
              label Coverage Dates
              input(type="text", ng-model="vm.collection.dates")
            md-input-container
              label Item Count
              input(type="text", ng-model="vm.collection.items")
            md-input-container
              label URL
              input(type="text", ng-model="vm.collection.url")
            md-input-container
              label Collection Group
              md-select(ng-model="vm.collection.category" placeholder="Select a Collection Group")
                md-option(ng-repeat="category in vm.categoryList" ng-value="category.id") {{category.title}}
            div(style="margin-bottom: 5px;")
              div.md-caption Item Type
              md-radio-group(layout="row" ng-model="vm.collection.ctype")
                div(flex="25")
                  md-radio-button(aria-label="Unrestricted" value="dig" style="display: inline") Collection
                div(flex="25")
                  md-radio-button(aria-label="Restricted" value="itm" style="display: inline") Single Item
                div(flex="25")
                  md-radio-button(aria-label="Restricted" value="aid" style="display: inline") Finding Aid
                span(flex)
            md-divider
            div(style="margin-top: 10px; margin-bottom: 15px;")
              div.md-caption Collection Access
              md-radio-group(layout="row" ng-model="vm.collection.restricted")
                div(flex="25")
                  md-radio-button(aria-label="Unrestricted" ng-value="false" flex="15" style="display: inline") Unrestricted
                div(flex="25")
                  md-radio-button(aria-label="Restricted" ng-value="true" flex="15" style="display: inline") Restricted
                  span(flex)
            md-divider
            div(style="margin-top: 10px; margin-bottom: 15px;")
              div.md-caption Browse Collection Type
              md-radio-group(layout="row" ng-model="vm.collection.browseType")
                div(flex="25")
                  md-radio-button(aria-label="Link" value="link" flex="15" style="display: inline") Link
                div(flex="25")
                  md-radio-button(aria-label="Selection" value="opts" flex="15" style="display: inline") Selection
                span(flex)
            md-divider
            div(style="margin-top: 10px; margin-bottom: 35px;")
              div.md-caption Search Options
              md-radio-group(layout="row" ng-model="vm.collection.repoType")
                div(flex="25")
                  md-radio-button(aria-label="Search & Browse" value="DEFAULT" flex="15" style="display: inline") Search & Browse
                div(flex="25")
                  md-radio-button(aria-label="Browse Only" value="BROWSE" flex="15" style="display: inline") Browse Only
                div(flex="25")
                  md-radio-button(aria-label="Search Only" value="SEARCH" flex="15" style="display: inline") Search Only
                span(flex)
  md-content(class="transparent" layout="column" flex="25" class="transparent")
    md-card.flex(flex)
      md-toolbar.md_primary
        .md-toolbar-tools
          i(class="material-icons") image
          h3.md-display-1 &nbsp;Image
          div(flex)
            md-button(class="md-accent md-raised md-fab md-mini", ng-click="vm.showDialog($event, vm.updateImageMessage)", style="float: right;")
              i(class="material-icons") file_upload
      md-card-content
        div(layout="row")
          md-card(flex="40" style="margin:0;max-width: 120px")
            img(style="max-width: 120px;" src="/resources/img/thumb/{{vm.thumbnailImage}}")
          md-card-content
            span.md-caption Image size 600px wide by 500px high (thumbnails created from original)

    subject-selector.flex

    content-type-selector.flex

    area-selector(flex ng-show="vm.areaUserId == 0")


